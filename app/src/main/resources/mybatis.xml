<?xml version="1.0" encoding="UTF-8" ?>
        <!DOCTYPE configuration
                PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
                "https://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <typeAliases>
        <package name="com.spw.rr.database"/>
        <package name="com.spw.rr.viewdb"/>
    </typeAliases>
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="${driver}"/>
                <property name="url" value="${url}"/>
                <property name="username" value="${username}"/>
                <property name="password" value="${password}"/>
            </dataSource>
        </environment>
        <environment id="test">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="org.h2.Driver"/>
                <property name="url" value="jdbc:h2:mem:testdb;INIT=create schema decoder\;set schema decoder\;runscript from 'c:/projects/decodder3/decoder/app/src/main/resources/createTables.sql'"/>
            </dataSource>
        </environment>
    </environments>
    <mappers>
        <mapper resource="com/spw/rr/database/Mapper.xml"/>
        <mapper resource="com/spw/rr/viewdb/ViewDb.xml"/>
    </mappers>
</configuration>