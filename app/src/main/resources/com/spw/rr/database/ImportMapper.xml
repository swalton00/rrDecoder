<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spw.rr.database.ImportMapper">
    <select id="getDBtime" resultType="java.sql.Timestamp">
        select current_timestamp()
        from dual;
    </select>
    <insert id="insertRosterEntry" parameterType="RosterEntry" useGeneratedKeys="true" keyColumn="id" keyProperty="id">
        INSERT INTO roster
        (entered,
         fullpath,
         fileDate,
         systemName)
        VALUES (#{entryTime},
                #{fullPath},
                #{fileDate},
                #{systemName})
    </insert>
    <update id="updateRosterEntry" parameterType="RosterEntry">
        UPDATE
            roster
        SET fullpath=#{fullPath},
            systemName = #{systemName},
            fileDate = #{fileDate},
            dateUpdated = #{dateUpdated}
        WHERE
            id = #{id}
    </update>
    <delete id="deleteDecoderEntry" parameterType="DecoderEntry">
        DELETE
        FROM Decoder
        WHERE id = #{id};
    </delete>
    <select id="getFunctionLabels" parameterType="int" resultMap="functionLabelMap">
        SELECT
            id,
            FUNCTIONNUMBER,
            FUNCTIONLABEL,
            Coalesce(LOCKED, 'false') as locked
            FROM FUNCTIONLABELS
        WHERE DECODERID = #{decoderId}
        ORDER BY FUNCTIONNUMBER
    </select>
    <resultMap id="functionLabelMap" type="FunctionLabel">
        <id property="id" column="id"/>
        <result property="functionNum" column="FunctionNumber"/>
        <result property="functionLabel" column="FunctionLabel"/>
        <result property="locked" column="Locked"/>
    </resultMap>
    <delete id="deleteOldLabels" parameterType="DecoderEntry">
        DELETE FROM FUNCTIONLABELS WHERE decoderId = #{id}
    </delete>
    <delete id="deleteOldKeys" parameterType="DecoderEntry">
        DELETE FROM KEYVALUES  WHERE decoderId = #{id}
    </delete>
    <select id="getLastVersion" resultType="VersionBase">
        SELECT
            decoderId,
            Version_Number as versionNumber
        FROM
            \${tableName} a
        WHERE
            decoderId = #{decoderId}
          AND version_number =
              (   SELECT
                      MAX(version_number)
                  FROM
                      \${tableName} b
                  WHERE
                      a.decoderId = b.decoderId)
    </select>
</mapper>